import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { ThemeProvider } from "@/components/theme-provider"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "EcoWeave Packaging | Sustainable WPP Bags Manufacturing",
  description:
    "Leading manufacturer of eco-friendly woven polypropylene bags for agricultural, industrial, and commercial use. Custom bulk packaging solutions with sustainable materials.",
  keywords:
    "WPP bags, woven polypropylene bags, custom bulk packaging, sustainable industrial bags, eco-friendly packaging",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider attribute="class" defaultTheme="light" enableSystem disableTransitionOnChange>
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}
